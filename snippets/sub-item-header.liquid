<div class="mobile--menu-header">
  <button class="mobile--menu-back">
    {%- render 'arrow--icon-back' -%}
    Back
  </button>
  {% comment %} title show  {% endcomment %}
  <div class="mobile--menu-item-title">{{ title }}</div>
  <button class="mobile--menu-close">
    <svg
      class="menu--Icon menu--Icon--close"
      role="presentation"
      viewBox="0 0 16 14">
      <path
        d="M15 0L1 14m14 0L1 0"
        stroke="currentColor"
        fill="none"
        fill-rule="evenodd"></path>
    </svg>
  </button>
</div>